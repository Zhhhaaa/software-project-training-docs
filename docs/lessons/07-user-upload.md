# 第 7 次课：新增用户与头像

## 开发任务

- 校验邮箱唯一性，按规则生成默认密码。
- 使用 `FormData` 同时提交文本字段和图片文件。
- 后端使用 `MultipartFile` 接收、校验并保存图片。
- 数据库只保存 `/uploads/文件名`，不保存 Base64 图片。
- Nginx 将 `/uploads/` 映射到上传目录。

## 必做边界

支持 jpg、jpeg、png；限制大小；使用 UUID 文件名；浏览器可访问上传图片。
